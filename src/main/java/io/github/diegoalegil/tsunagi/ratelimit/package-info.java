/**
 * Rate limiting: a token bucket limiter that keeps request rates within the
 * limits imposed by public APIs.
 */
@NullMarked
package io.github.diegoalegil.tsunagi.ratelimit;

import org.jspecify.annotations.NullMarked;
